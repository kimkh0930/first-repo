# sample code

```python
from random import randint 


def num_picker(num):
    start_num = 1
    if num >= 1 and num < 46:
       end_num = num + 1
       result = randint(1,num+1)
    return result
```
